# Backlog — функции в развитие

Копилка идей вне скоупа: записываем, не реализуем, пока Олег не скажет
«делаем».

> **Точка истины для актуальной реализации — `REQUIREMENTS.md`.**
> Этот файл — только копилка идей под развитие. Когда идея берётся в работу,
> она переезжает в `REQUIREMENTS.md` (и/или в бриф фазы), а отсюда удаляется.

---

- **Неймспейсы (иерархические)** (REQUIREMENTS §9 «развитие»; концепция
  согласована с О. 2026-09-03 — «кладём в бэклог и продумываем»). Лёгкие
  «папки» для заметок: их мало (3–7), они крупные, выбор однозначен — в
  отличие от тегов дисциплина почти бесплатная. Концепция:
  - **Модель данных**: `notes.namespace TEXT DEFAULT 'default'` + индекс;
    таблица-реестр `namespaces` (`path`, `description` — обязательное,
    счётчики). Максимум **2 уровня** (`work`, `work/sbos2020`) — глубже свалка.
    Существующие заметки → `default` (миграция со снапшотом).
  - **Контракты**: опциональный `namespace` в save/update/search/list;
    метка `namespace` в выдачах search/list/get; новый инструмент
    `memory_namespaces` (реестр: путь, описание, заметок, свежесть).
  - **Ориентирование модели — 3 слоя**: (1) карта неймспейсов с описаниями
    в системных инструкциях MCP (статична до reconnect); (2)
    `memory_namespaces` по требованию; (3) метки неймспейсов в результатах —
    модель учится на лету. Правило: уверен в области — ищи с namespace;
    не уверен — глобально и сужай (промах ничего не теряет).
  - **Рекурсивный поиск по умолчанию**: запрос по узлу = поддерево
    (`work` покрывает `work/sbos2020`) — иерархия прощает неточность уровня
    вниз. Точный фильтр узла — опция. Векторная ветка: sqlite-vec vec0
    partition keys по namespace, prefix-фильтр списком узлов поддерева.
  - **Дедуп** — в пределах неймспейса; при близком дубле в другом — хинт
    «похожее есть в X» (сигнал ориентирования + защита от копий).
  - **Дисциплина создания листов** — не авансом: лист заводится при
    накоплении массы (ориентир ≥10–15 заметок по проекту) или явной
    изоляции домена. Запись — в лист, если есть; общее для домена — в корень.
  - **Этапность**: этап 1 — ядро (модель данных, параметры, реестр,
    `memory_namespaces`, карта в инструкциях, метки в выдачах, миграция);
    этап 2 (опционально, позже) — авто-роутинг: search без namespace сужает
    область по сходству запроса с описаниями неймспейсов, fallback глобальный.
  - **Стартовый набор** (пример, О. 2026-09-03): `default`, `work`,
    `work/sbos2020`, `work/is1777`, `projects`, `projects/llmsecondbrain` —
    финальный список при реализации.

- **Вынос LLM-промптов в env** (идея ассистента, 2026-08-31). Сейчас системные
  промпты суммаризации, слияния дублей и судьи дедупа захардкожены как модульные
  константы в `app/services/summary.py` (`SYSTEM_PROMPT`, `MERGE_SYSTEM_PROMPT`,
  `MERGE_USER_TEMPLATE`) и `app/services/judge.py` (`JUDGE_SYSTEM_PROMPT`,
  `JUDGE_USER_TEMPLATE`). Вынести в переменные окружения с дефолтами = текущие
  тексты, чтобы менять формулировки без пересборки/передеплоя:
  `SUMMARY_SYSTEM_PROMPT`, `MERGE_SYSTEM_PROMPT`, `MERGE_USER_TEMPLATE`,
  `DEDUP_JUDGE_SYSTEM_PROMPT`, `DEDUP_JUDGE_USER_TEMPLATE`. Вопросы к будущему
  проектированию (пока не решения): выносить ли user-шаблоны — это format-строки
  с плейсхолдерами (`{text_a}`/`{text_b}`/`{text_new}`/`{text_candidate}`),
  которые пользователь env обязан сохранить; многострочность значений в
  docker-compose (block scalar `|`). Реализация = поля `Settings` с валидацией
  непустоты + правки `_payload`/`_chat` в обоих сервисах + `OPTIONAL_ENV` в
  `tests/test_config.py` (completeness-тест сверяет `Settings.model_fields`
  ровно с `OPTIONAL_ENV + REQUIRED_ENV`) + docker-compose + документация
  (§8, ARCHITECTURE §4.7, README).

---

## Отклонено

- **Теги заметок** (идея Олега 2026-08-30) — отклонено им же 2026-09-03: кейс
  поиска закрывает гибридный поиск (вектора + FTS, порог 0.50); теги требуют
  ручной дисциплины при `memory_save` и не окупаются на текущем объёме. Если
  появится потребность в структурной фильтрации — неймспейсы (REQUIREMENTS §9),
  не теги.
